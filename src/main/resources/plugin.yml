name: GoodVibesOnly # the plugins name as it should appear in the plugin list /pl
version: 1.0 # the plugin's version
author: Jesse Thomas
main: me.Jesse.SpigotGradleProject.Main
api-version: # the version of the API you want to use, required starting with 1.13